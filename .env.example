# --- Local-only (one PC, no cloud server): only this machine can reach the API.
ACTIVITY_HOST=127.0.0.1
# --- Hosted / LAN: listen on all interfaces (use firewall + strong API key; prefer HTTPS on the internet).
# ACTIVITY_HOST=0.0.0.0

ACTIVITY_PORT=8000
ACTIVITY_API_KEY=change-me
ACTIVITY_DB_PATH=./data/activity.db
